# লোকাল ডেভেলপমেন্ট সেটআপ — HTTPS + মোবাইল অ্যাক্সেস (PWA)

## সমস্যা কী?

Progressive Web App (PWA) এর জন্য **HTTPS** বাধ্যতামূলক (localhost ছাড়া)। আবার মোবাইল থেকে PC-র localhost অ্যাক্সেস করতে হলে PC-র **Local IP** দিয়ে অ্যাক্সেস করতে হয়, যা browser-এর কাছে localhost নয় — তাই HTTPS ছাড়া PWA কাজ করবে না।

**আমাদের লক্ষ্য:**
1. PC-তে localhost-এ কাজ করা ও আউটপুট দেখা
2. মোবাইল থেকে একই সাইট ব্রাউজ করা (same Wi-Fi network-এ)
3. HTTPS থাকা যেন PWA সব features (install prompt, service worker, offline) ঠিকমতো কাজ করে

---

## সমাধান: Valet for Windows (Recommended) বনাম অন্যান্য পদ্ধতি

| পদ্ধতি | HTTPS | মোবাইল অ্যাক্সেস | সেটআপ জটিলতা | PWA সাপোর্ট |
|---|---|---|---|---|
| **Valet for Windows** | ✅ Auto SSL | ✅ (কিছু কনফিগ লাগে) | মাঝারি | ✅ |
| **ngrok** | ✅ Auto SSL | ✅ (যেকোনো জায়গা থেকে) | সহজ | ✅ |
| **XAMPP + mkcert** | ✅ Manual SSL | ✅ (phone-এ cert install লাগে) | জটিল | ✅ |
| **Vite HTTPS Plugin** | ✅ Dev server only | ⚠️ (শুধু assets) | সহজ | ⚠️ আংশিক |

---

## পদ্ধতি ১: ngrok ব্যবহার (সবচেয়ে সহজ ও দ্রুত) ⭐

**ngrok** আপনার localhost-কে একটি public HTTPS URL দেয়। কোনো certificate ইনস্টল করা লাগে না। PC ও মোবাইল উভয় থেকে কাজ করে।

### ধাপ ১: ngrok ইনস্টল

```powershell
# winget দিয়ে (Windows 10/11):
winget install ngrok.ngrok

# অথবা https://ngrok.com/download থেকে ডাউনলোড করে extract করুন
```

### ধাপ ২: ngrok অ্যাকাউন্ট তৈরি ও auth token সেট

1. https://dashboard.ngrok.com/signup এ গিয়ে ফ্রি অ্যাকাউন্ট করুন
2. Dashboard থেকে **auth token** কপি করুন
3. টার্মিনালে চালান:

```powershell
ngrok config add-authtoken YOUR_AUTH_TOKEN
```

### ধাপ ৩: Laravel সার্ভার চালু করুন

```powershell
cd c:\xampp\htdocs\Laravel-Practice\PwaPractice
php artisan serve --host=0.0.0.0 --port=8000
```

### ধাপ ৪: আরেকটি টার্মিনালে ngrok চালু করুন

```powershell
ngrok http 8000
```

আউটপুটে এমন কিছু দেখবেন:

```
Forwarding    https://abc123.ngrok-free.app -> http://localhost:8000
```

### ধাপ ৫: মোবাইলে ব্রাউজ করুন

`https://abc123.ngrok-free.app` — এই URL মোবাইলে ওপেন করুন। HTTPS সহ PWA ঠিকমতো কাজ করবে।

### ⚠️ গুরুত্বপূর্ণ: Laravel Trusted Proxies কনফিগার করুন

ngrok ব্যবহার করলে Laravel-এর `APP_URL` ও trusted proxies ঠিক করতে হবে:

**`.env` ফাইলে (শুধু লোকাল ডেভেলপমেন্টে):**
```env
APP_URL=https://abc123.ngrok-free.app
```

> ⚠️ ngrok প্রতিবার চালু করলে URL পরিবর্তন হয় (ফ্রি প্ল্যানে)। তাই প্রতিবার `.env` আপডেট করতে হবে।

### ngrok এর সুবিধা ও সীমাবদ্ধতা

**সুবিধা:**
- সবচেয়ে সহজ সেটআপ
- কোনো SSL certificate ম্যানুয়ালি তৈরি করা লাগে না
- মোবাইলে কোনো certificate ইনস্টল করা লাগে না
- যেকোনো নেটওয়ার্ক থেকে অ্যাক্সেস করা যায়

**সীমাবদ্ধতা (ফ্রি প্ল্যান):**
- প্রতিবার নতুন URL তৈরি হয়
- Rate limit আছে
- ব্রাউজারে প্রথমবার একটি warning page আসতে পারে ("Visit Site" ক্লিক করতে হবে)

---

## পদ্ধতি ২: mkcert + XAMPP (ফুল কন্ট্রোল, স্থায়ী সেটআপ) 🔧

এই পদ্ধতিতে আপনার নিজের locally-trusted SSL certificate তৈরি হবে। URL স্থায়ী থাকবে।

### ধাপ ১: mkcert ইনস্টল

```powershell
# winget দিয়ে:
winget install FiloSottile.mkcert

# অথবা chocolatey দিয়ে:
choco install mkcert
```

### ধাপ ২: Local CA ইনস্টল ও Certificate তৈরি

```powershell
# Local Certificate Authority (CA) ইনস্টল করুন
mkcert -install

# আপনার PC-র Local IP বের করুন (Wi-Fi adapter দেখুন)
ipconfig
# ধরুন আপনার IP: 192.168.0.105

# Certificate তৈরি করুন
cd c:\xampp\htdocs\Laravel-Practice\PwaPractice
mkcert localhost 127.0.0.1 192.168.0.105
```

এটি দুটি ফাইল তৈরি করবে:
- `localhost+2.pem` (certificate)
- `localhost+2-key.pem` (private key)

### ধাপ ৩: XAMPP Apache-এ SSL কনফিগার করুন

**`C:\xampp\apache\conf\extra\httpd-vhosts.conf`** ফাইলে যোগ করুন:

```apache
# HTTP → HTTPS Redirect
<VirtualHost *:80>
    ServerName pwa.local
    Redirect permanent / https://pwa.local/
</VirtualHost>

# HTTPS VirtualHost
<VirtualHost *:443>
    ServerName pwa.local
    DocumentRoot "c:/xampp/htdocs/Laravel-Practice/PwaPractice/public"

    SSLEngine on
    SSLCertificateFile "c:/xampp/htdocs/Laravel-Practice/PwaPractice/localhost+2.pem"
    SSLCertificateKeyFile "c:/xampp/htdocs/Laravel-Practice/PwaPractice/localhost+2-key.pem"

    <Directory "c:/xampp/htdocs/Laravel-Practice/PwaPractice/public">
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>
```

### ধাপ ৪: Windows Hosts ফাইলে ডোমেইন যোগ করুন

**`C:\Windows\System32\drivers\etc\hosts`** ফাইলে (Administrator হিসেবে edit করুন):

```
127.0.0.1    pwa.local
```

### ধাপ ৫: `.env` আপডেট করুন

```env
APP_URL=https://pwa.local
```

### ধাপ ৬: XAMPP Apache রিস্টার্ট করুন

XAMPP Control Panel থেকে Apache Stop → Start করুন।

এখন `https://pwa.local` দিয়ে PC-তে অ্যাক্সেস করতে পারবেন।

### ধাপ ৭: মোবাইল থেকে অ্যাক্সেস (এটিই মূল চ্যালেঞ্জ)

মোবাইল থেকে অ্যাক্সেস করতে হলে phone-এ mkcert এর **Root CA certificate** ইনস্টল করতে হবে:

1. **Root CA খুঁজে বের করুন:**
   ```powershell
   mkcert -CAROOT
   # আউটপুট: C:\Users\YourName\AppData\Local\mkcert
   ```

2. **`rootCA.pem` ফাইলটি phone-এ পাঠান** (email, USB, cloud drive ইত্যাদি দিয়ে)

3. **Android-এ CA Certificate ইনস্টল:**
   - Settings → Security → Encryption & credentials → Install a certificate → CA certificate
   - `rootCA.pem` ফাইলটি সিলেক্ট করুন ও ইনস্টল করুন

4. **iPhone-এ CA Certificate ইনস্টল:**
   - ফাইলটি open করুন → Profile install করুন
   - Settings → General → About → Certificate Trust Settings → root certificate enable করুন

5. **মোবাইলের ব্রাউজারে যান:**
   ```
   https://192.168.0.105
   ```
   (আপনার PC-র IP দিয়ে অ্যাক্সেস করুন)

> ⚠️ মোবাইল ও PC একই Wi-Fi নেটওয়ার্কে থাকতে হবে।

---

## পদ্ধতি ৩: `php artisan serve` + Vite HTTPS (দ্রুত ডেভেলপমেন্ট) 🚀

শুধু PC-তে দ্রুত dev করতে চাইলে এটি সবচেয়ে লাইটওয়েট পদ্ধতি। তবে **মোবাইলে HTTPS PWA টেস্ট করার জন্য এটি যথেষ্ট নয়**।

### ধাপ ১: `@vitejs/plugin-basic-ssl` ইনস্টল

```powershell
cd c:\xampp\htdocs\Laravel-Practice\PwaPractice
npm install -D @vitejs/plugin-basic-ssl
```

### ধাপ ২: `vite.config.js` আপডেট

```javascript
import { defineConfig } from 'vite';
import laravel from 'laravel-vite-plugin';
import tailwindcss from '@tailwindcss/vite';
import basicSsl from '@vitejs/plugin-basic-ssl';

export default defineConfig({
    plugins: [
        laravel({
            input: ['resources/css/app.css', 'resources/js/app.js'],
            refresh: true,
        }),
        tailwindcss(),
        basicSsl(),
    ],
    server: {
        https: true,
        host: '0.0.0.0',
        watch: {
            ignored: ['**/storage/framework/views/**'],
        },
    },
});
```

### ধাপ ৩: চালু করুন

```powershell
# Terminal 1:
php artisan serve

# Terminal 2:
npm run dev
```

> ⚠️ এটি শুধু Vite dev server-এ HTTPS দেয়। Laravel সার্ভার নিজে HTTP-তেই থাকে। PC-র `localhost:8000`-এ PWA **কিছু features** কাজ করবে (Chrome localhost-কে secure context হিসেবে ট্রিট করে), কিন্তু মোবাইলে IP দিয়ে অ্যাক্সেস করলে full PWA কাজ করবে না।

---

## আমার সুপারিশ (Recommendation) 💡

| আপনার প্রয়োজন | সুপারিশকৃত পদ্ধতি |
|---|---|
| **দ্রুত মোবাইলে টেস্ট করতে চান** | পদ্ধতি ১: **ngrok** (সবচেয়ে সহজ) |
| **স্থায়ী লোকাল সেটআপ চান, URL পরিবর্তন হবে না** | পদ্ধতি ২: **mkcert + XAMPP** |
| **শুধু PC-তে dev করবেন, মোবাইল দরকার নেই** | পদ্ধতি ৩: **Vite HTTPS** বা সরাসরি `localhost:8000` |

### দ্রুত শুরু করতে চাইলে (Quick Start):

**ngrok দিয়ে ৩ মিনিটেই শুরু করুন:**

```powershell
# Terminal 1 — Laravel চালু করুন:
cd c:\xampp\htdocs\Laravel-Practice\PwaPractice
php artisan serve --host=0.0.0.0 --port=8000

# Terminal 2 — ngrok চালু করুন:
ngrok http 8000
```

তারপর ngrok-এর দেওয়া `https://...` URL মোবাইলে ওপেন করুন। ব্যস! ✅

---

## গুরুত্বপূর্ণ নোট

1. **Git Push-এর আগে** `.env` ফাইলের `APP_URL` পুরনো মানে (production URL) ফিরিয়ে রাখুন, অথবা `.env` gitignore-এ আছে কিনা নিশ্চিত করুন (ডিফল্টভাবে থাকে)।
2. **Service Worker ক্যাশ** পরিবর্তনের পর ব্রাউজারে hard refresh (`Ctrl+Shift+R`) বা DevTools → Application → Service Workers → Update করুন।
3. **Windows Firewall** — মোবাইল থেকে অ্যাক্সেস না হলে Windows Firewall-এ port 8000 allow করুন:
   ```powershell
   # PowerShell (Admin):
   New-NetFirewallRule -DisplayName "Laravel Dev Server" -Direction Inbound -Protocol TCP -LocalPort 8000 -Action Allow
   ```
4. **PC-র IP বের করুন:**
   ```powershell
   ipconfig
   # "Wireless LAN adapter Wi-Fi" সেকশনে IPv4 Address দেখুন
   # সাধারণত 192.168.x.x ফরম্যাটে হয়
   ```
